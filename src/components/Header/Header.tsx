import React from "react"
import s from "./Header.module.scss"

export const Header = () => {
    return (
        <header className={s.headerBlock}>
            <b>H E A D E R</b>
        </header>
    )
}